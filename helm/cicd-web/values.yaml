replicaCount: 2

image:
  repository: ghcr.io/odedbar22/cicd-lab
  tag: "set-by-pipeline"
  pullPolicy: Always

imagePullSecrets:
  - name: ghcr-pull

service:
  port: 80

resources:
  requests:
    cpu: 100m
    memory: 64Mi
  limits:
    cpu: 500m
    memory: 128Mi
